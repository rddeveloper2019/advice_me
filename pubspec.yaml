name: advice_me
description: "Advice of the day"

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  auto_route: ^11.1.0
  dio: ^5.9.2
  retrofit: ^4.9.0
  json_annotation: ^4.9.0
  flutter_dotenv: ^6.0.0
  flutter_bloc: ^9.1.1
  equatable: 2.0.8
  translator: ^1.0.4+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  auto_route_generator: 10.5.0
  retrofit_generator: ^10.0.1
  json_serializable: ^6.10.0
  build_runner: 2.12.2

flutter:
  uses-material-design: true
  assets:
    - .env
